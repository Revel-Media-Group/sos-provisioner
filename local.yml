---
- hosts: localhost
  become: true
  tasks:
    - tags:
        - never
        - patcher
      block:
        - name: patcher | ubuntu
          include_role:
            name: patcher/ubuntu
          tags:
            - never
            - ubuntu

        - name: patcher | fedora
          include_role:
            name: patcher/fedora
          tags:
            - never
            - fedora

        - name: patcher | rescue
          command: command -v asdf &> /dev/null
          tags:
            - never
            - rescue
      rescue:
        - mail:
            host: '{{ mailchimp_host }}'
            port: '{{ mailchimp_port }}'
            username: '{{ mailchimp_user }}'
            password: '{{ mailchimp_pass }}'
            to: '{{ mailchimp_to }}'
            from: '{{ mailchimp_from }}'
            subject: Ansible Role Execution Failed
            body: test
            secure: starttls

    - name: provision
      tags:
        - never
        - provision
      block:
        - name: provision | ubuntu | official
          tags:
            - never
            - ubuntu
            - official
          include_role:
            name: provision/ubuntu/sos-official

        - name: provision | ubuntu | custom
          tags:
            - never
            - ubuntu
            - custom
          include_role:
            name: provision/ubuntu/sos-custom

        - name: provision | fedora | official
          tags:
            - never
            - fedora
            - official
          include_role:
            name: provision/fedora/sos-official

        - name: provision | fedora | custom
          tags:
            - never
            - fedora
            - custom
          include_role:
            name: provision/fedora/sos-custom
